apiVersion: v2
appVersion: v0.0.2
dependencies:
  - name: tpl-argocd-baseline
    repository: file://../tpl-argocd-baseline
    condition: tpl-argocd-baseline.enabled
  - name: cert-manager-issuer
    repository: file://../cert-manager-issuer
    condition: cert-manager-issuer.enabled
name: seed-baseline
sources:
  - https://github.com/stakater/Forecastle
version: 1.0.0