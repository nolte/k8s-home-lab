name: "Lint: Terraform"

on:
  push:
    branches: [ master ]
  pull_request:


jobs:
  lint:
    uses: nolte/gh-plumbing/.github/workflows/reusable-tf-lint.yaml@v2.2.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
